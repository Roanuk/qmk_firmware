#ifndef CONFIG_ROANUK_H
#define CONFIG_ROANUK_H

#define RGB_MATRIX_KEYPRESSES
#define ONESHOT_TAP_TOGGLE 2  /* Tapping this number of times holds the key until tapped this number of times again. */
#define ONESHOT_TIMEOUT 30000  /* Time (in ms) before the one shot key is released */
#endif